version: 1.0.0
info: { title: T, table_name: t }
model:
  fields:
    - { name: id, type: integer, required: true }
    - { name: s, type: string }
    - { name: parts, type: string }
transformations:
  - phase: post
    split: { field: s, delimiter: "_", output: parts }
