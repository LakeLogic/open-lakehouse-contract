version: 1.0.0
info: { title: T, table_name: t }
model:
  fields:
    - { name: id, type: integer, required: true }
    - { name: tag, type: string }
transformations:
  - phase: pre
    explode: { field: tags, output: tag }
