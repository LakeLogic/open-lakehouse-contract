id: OLC-T-016
spec_version: 1.0.0
level: core-runtime
feature: transformations.explode
description: explode expands a list column into one row per element, identically across engines.
input: input.jsonl
expected:
  accepted: expected/accepted.jsonl
assertions:
  rows_input: 2
  rows_accepted: 3
comparison:
  sort_by: [id, tag]
