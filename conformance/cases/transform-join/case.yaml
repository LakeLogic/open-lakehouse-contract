id: OLC-T-015
spec_version: 1.0.0
level: core-runtime
feature: transformations.join
description: join enriches rows with multiple columns from a reference link table (LEFT JOIN), identically across engines.
input: input.jsonl
expected:
  accepted: expected/accepted.jsonl
assertions:
  rows_input: 2
  rows_accepted: 2
comparison:
  sort_by: [id]
