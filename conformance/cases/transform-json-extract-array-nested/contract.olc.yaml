version: 1.0.0
info: { title: T, table_name: t }
model:
  fields:
    - { name: id, type: integer }
    - { name: payload, type: string }
    - { name: code, type: string }
transformations:
  - phase: post
    json_extract: { field: code, source: payload, path: "$.items[1].meta.code", cast: string }
