version: 1.0.0
info:
  title: Orders
  table_name: orders
model:
  fields:
    - { name: order_id, type: integer, required: true }
    - { name: line_no, type: integer, required: true }
quality:
  dataset_rules:
    - { name: order_line_unique, unique: [order_id, line_no] }
