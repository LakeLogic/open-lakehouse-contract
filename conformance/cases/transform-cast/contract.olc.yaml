version: 1.0.0
info: { title: T, table_name: t }
model:
  fields:
    - { name: id, type: integer, required: true }
    - { name: amount, type: float }
transformations:
  - phase: post
    cast: { columns: { amount: string } }
