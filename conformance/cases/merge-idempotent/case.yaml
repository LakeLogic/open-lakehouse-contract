id: OLC-MERGE-002
spec_version: 1.0.0
level: materialization
feature: materialization.merge
description: Re-running the identical merge leaves the target unchanged.
input: input.jsonl
expected:
  target: expected/target.jsonl
assertions:
  rows_accepted: 2
comparison:
  sort_by:
  - id
materialization:
  format: delta
  seed: seed.jsonl
  idempotent: true
