version: 1.0.0
info: { title: T, table_name: t }
model:
  fields:
    - { name: id, type: integer, required: true }
    - { name: a, type: string }
transformations:
  - phase: post
    drop: { columns: [b] }
