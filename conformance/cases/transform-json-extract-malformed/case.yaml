id: OLC-T-010f
spec_version: 1.0.0
level: core-runtime
feature: transformations.json_extract
description: A malformed JSON document yields NULL for that row and does not abort the run
input: input.jsonl
expected:
  accepted: expected/accepted.jsonl
assertions:
  rows_input: 2
  rows_accepted: 2
comparison:
  sort_by: [id]
